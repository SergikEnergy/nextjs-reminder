import { Inter, Lusitana } from 'next/font/google';

export const inter = Inter({ style: 'normal', subsets: ['latin'] });
export const lusitana = Lusitana({ subsets: ['latin'], weight: ['400', '700'] });
